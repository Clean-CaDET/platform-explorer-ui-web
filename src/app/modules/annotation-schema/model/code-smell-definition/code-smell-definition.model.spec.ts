import { CodeSmellDefinition } from './code-smell-definition.model';

describe('CodeSmellDefinition', () => {
  it('should create an instance', () => {
    expect(new CodeSmellDefinition()).toBeTruthy();
  });
});
