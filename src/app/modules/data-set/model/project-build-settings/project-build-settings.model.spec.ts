import { ProjectBuildSettings } from './project-build-settings.model';

describe('ProjectBuildSettings', () => {
  it('should create an instance', () => {
    expect(new ProjectBuildSettings()).toBeTruthy();
  });
});
