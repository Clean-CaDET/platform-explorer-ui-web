import { DatasetDetailDTO } from "./dataset-detail-dto.model";

describe('DatasetDetailDTO', () => {
  it('should create an instance', () => {
    expect(new DatasetDetailDTO()).toBeTruthy();
  });
});
