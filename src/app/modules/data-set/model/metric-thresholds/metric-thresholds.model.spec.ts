import { MetricThresholds } from './metric-thresholds.model';

describe('MetricThresholds', () => {
  it('should create an instance', () => {
    expect(new MetricThresholds()).toBeTruthy();
  });
});
