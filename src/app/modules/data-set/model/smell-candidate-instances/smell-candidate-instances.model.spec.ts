import { SmellCandidateInstances } from './smell-candidate-instances.model';

describe('SmellCandidateInstances', () => {
  it('should create an instance', () => {
    expect(new SmellCandidateInstances()).toBeTruthy();
  });
});
